services:
  whatsapp-web-bridge:
    build: .
    container_name: bhg-whatsapp-web-bridge
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "3000:3000"
    volumes:
      - ./data:/data
